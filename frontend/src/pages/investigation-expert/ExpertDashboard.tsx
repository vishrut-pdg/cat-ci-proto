import { useEffect,useState } from "react";import { Link } from "react-router";import { apiGet } from "../../services/api";import { K,PageTitle } from "../finance-analyst/FinanceDashboard";
interface I{id:string;opportunity_id:string;status:string;due_at:string;part_name:string;plant_name:string;potential_savings:number;priority:string}
const value=(n:number)=>new Intl.NumberFormat("en-US",{style:"currency",currency:"USD",notation:"compact"}).format(n);
export default function ExpertDashboard(){const[items,setItems]=useState<I[]>([]);useEffect(()=>{apiGet<{items:I[]}>("/investigations").then(x=>setItems(x.items))},[]);return <main><PageTitle title="Investigation Expert Dashboard" sub="Validate root causes, collaborate with specialists, and submit evidence-backed recommendations."/><div className="kpis"><K label="My Investigations" value={items.length} note="Active assignments"/><K label="Investigation Pipeline" value={items.filter(x=>x.status==="IN_PROGRESS").length} note="In evidence review" tone="amber"/><K label="Due This Week" value={items.filter(x=>new Date(x.due_at).getTime()-Date.now()<604800000).length} note="Needs attention" tone="red"/><K label="Expert Network" value={14} note="Available specialists" tone="green"/></div><section className="panel"><div className="panel-head"><div><h2>Investigation pipeline</h2><p>Decision-ready context across your active workload.</p></div><Link className="button-link" to="/investigation-expert/my-investigations">View all investigations →</Link></div><div className="pipeline">{items.slice(0,6).map(x=><Link key={x.id} to={`/investigation-expert/my-investigations/${x.opportunity_id}`}><b>{x.part_name}</b><span>{x.plant_name}</span><div className="pipeline-metadata"><span><small>STATUS</small><b>{x.status.replaceAll("_"," ")}</b></span><span><small>PRIORITY</small><b>{x.priority}</b></span><span><small>DUE</small><b>{new Date(x.due_at).toLocaleDateString("en-GB",{day:"2-digit",month:"short"})}</b></span><span><small>VALUE</small><b>{value(x.potential_savings)}</b></span></div></Link>)}</div></section></main>}
